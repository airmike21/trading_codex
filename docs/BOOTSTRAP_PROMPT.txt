You are continuing the Trading Codex project in WSL Ubuntu.

Read and follow `docs/ASSISTANT_BRIEF.md` first.

Current project state:
- Data updater work is complete.
- Daily runner / next-action alert workflow work is complete and validated.
- Do not reopen that plumbing unless a regression appears.
- The next implementation priority is the narrow clarified Stage 2 paper-execution path: implement a minimal IBKR PaperTrader operational acceptance path for the primary live candidate. Do not reopen Stage 3 strategy work or generic broker abstraction by default.

Hard rules:
- `--next-action-json` = exactly one minified stdout line.
- `--next-action` = exactly one human stdout line.
- `next_action_alert` prints one line only on emit and nothing if unchanged.
- `event_id` composition must not change.
- Do not commit directly to `master` unless explicitly instructed.
- Preserve uncommitted PS1 work in `scripts/windows/trading_codex_next_action_alert.ps1`.
- Keep `configs/presets.json` local-only and uncommitted.
